---
phase: 01-build-baseline-recovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .bazelrc
  - ported_clickhouse/BUILD.bazel
  - ported_clickhouse/parsers/BUILD.bazel
  - ported_clickhouse/common/BUILD.bazel
  - ported_clickhouse/base/BUILD.bazel
  - examples/bootstrap/BUILD.bazel
requirements:
  - BUILD-01
  - BUILD-03
autonomous: true
must_haves:
  truths:
    - "Bazel can evaluate and resolve parser-related labels on Bazel 9 without native cc_* errors."
    - "A clean checkout can build //ported_clickhouse:parser_lib without machine-local compiler-wrapper paths."
  artifacts:
    - path: ".bazelrc"
      provides: "Portable compiler/toolchain configuration without required absolute local wrapper paths."
      contains: "repo_env"
    - path: "ported_clickhouse/BUILD.bazel"
      provides: "Bazel 9-compatible rules_cc symbol loading for parser library targets."
      contains: "@rules_cc//cc:defs.bzl"
    - path: "examples/bootstrap/BUILD.bazel"
      provides: "Bazel 9-compatible rules_cc symbol loading for smoke targets."
      contains: "@rules_cc//cc:defs.bzl"
  key_links:
    - from: "ported_clickhouse/BUILD.bazel"
      to: "//ported_clickhouse:parser_lib"
      via: "rules_cc load statements enable cc_* rule resolution during BUILD evaluation"
      pattern: "load\\(\"@rules_cc//cc:defs\\.bzl\""
    - from: "examples/bootstrap/BUILD.bazel"
      to: "//examples/bootstrap:use_parser_smoke"
      via: "rules_cc load statements keep smoke target graph parseable on Bazel 9"
      pattern: "load\\(\"@rules_cc//cc:defs\\.bzl\""
    - from: ".bazelrc"
      to: "//ported_clickhouse:parser_lib"
      via: "default Bazel invocation avoids mandatory machine-local CC/CXX wrapper paths"
      pattern: "repo_env=(CC|CXX)="
---

# Plan 01-01: Bazel 9 Baseline Unblockers

<objective>
Unblock clean-checkout Bazel target resolution and parser library build on current Bazel by fixing removed native `cc_*` symbol usage and non-portable compiler-wrapper configuration.

Purpose: Restore a deterministic, portable baseline for Phase 1 execution.
Output: Bazel-9-compatible BUILD files and portable `.bazelrc` defaults.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add rules_cc loads to all in-scope BUILD files using cc_* rules</name>
  <files>ported_clickhouse/BUILD.bazel, ported_clickhouse/parsers/BUILD.bazel, ported_clickhouse/common/BUILD.bazel, ported_clickhouse/base/BUILD.bazel, examples/bootstrap/BUILD.bazel</files>
  <action>Add `load("@rules_cc//cc:defs.bzl", ...)` where `cc_library`/`cc_binary` are declared. Keep target names and graph unchanged; this is compatibility-only.</action>
  <verify>
    <automated>bazel --batch --output_user_root=/tmp/bazel-root query //ported_clickhouse:parser_lib</automated>
  </verify>
  <done>All in-scope BUILD files parse on Bazel 9 and key parser label resolution succeeds for `BUILD-01`.</done>
</task>

<task type="auto">
  <name>Task 2: Validate smoke label parse health with fast query checks</name>
  <files>examples/bootstrap/BUILD.bazel</files>
  <action>Run label-level query checks for smoke targets to catch parse regressions quickly before full builds/tests.</action>
  <verify>
    <automated>bazel --batch --output_user_root=/tmp/bazel-root query //examples/bootstrap:use_parser_smoke //examples/bootstrap:select_lite_smoke //examples/bootstrap:select_from_lite_smoke //examples/bootstrap:select_rich_smoke</automated>
  </verify>
  <done>All four smoke labels resolve without BUILD evaluation errors, improving feedback latency for BUILD graph regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Remove required machine-local CC/CXX paths from tracked Bazel config</name>
  <files>.bazelrc</files>
  <action>Replace absolute `repo_env=CC/CXX=...` defaults with workspace-portable behavior. Keep optional sanitizer-wrapper usage behind explicit opt-in config only.</action>
  <verify>
    <automated>rg -n "clickhouse-patching|/Users/|repo_env=(CC|CXX)=" .bazelrc</automated>
  </verify>
  <done>Tracked default config no longer requires machine-local absolute compiler-wrapper paths, satisfying portability scope for `BUILD-03`.</done>
</task>

<task type="auto">
  <name>Task 4: Confirm parser library still builds with updated portable config</name>
  <files>.bazelrc, ported_clickhouse/BUILD.bazel</files>
  <action>Run a deterministic parser-lib build on current Bazel to verify compatibility and portability edits together, then prove the same command works from a clean checkout baseline.</action>
  <verify>
    <automated>bazel --batch --output_user_root=/tmp/bazel-root build //ported_clickhouse:parser_lib</automated>
    <automated>git worktree add /tmp/clickshack-phase01-clean HEAD && (cd /tmp/clickshack-phase01-clean && bazel --batch --output_user_root=/tmp/bazel-root-clean build //ported_clickhouse:parser_lib) && git worktree remove /tmp/clickshack-phase01-clean</automated>
  </verify>
  <done>`//ported_clickhouse:parser_lib` builds successfully in both active workspace and clean-checkout worktree, providing task-level proof that `BUILD-01` is restored from fresh baseline.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `bazel --batch --output_user_root=/tmp/bazel-root query //ported_clickhouse:parser_lib` passes
- [ ] smoke target query group passes in one command
- [ ] `bazel --batch --output_user_root=/tmp/bazel-root build //ported_clickhouse:parser_lib` passes
- [ ] clean-checkout worktree build proof for `//ported_clickhouse:parser_lib` passes
</verification>

<success_criteria>
- All tasks completed
- Bazel 9 parse/build baseline recovered for parser path
- Tracked config is portable and not machine-path-coupled
</success_criteria>
