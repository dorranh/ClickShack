---
phase: 01-build-baseline-recovery
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Justfile
  - tools/
  - examples/bootstrap/BUILD.bazel
  - .planning/phases/01-build-baseline-recovery/01-VALIDATION.md
requirements:
  - BUILD-02
  - BUILD-03
  - BUILD-01
autonomous: true
must_haves:
  truths:
    - "All four parser smoke targets pass in a deterministic Bazel test run."
    - "Build/test flow remains reproducible from clean checkout and does not require tracked source from tmp/."
    - "Build/test flow succeeds even when workspace `tmp/` is absent."
    - "Parser library build stays green in the same reproducibility run."
  artifacts:
    - path: "Justfile"
      provides: "Single repeatable smoke-suite entrypoint with deterministic Bazel commands."
      contains: "use_parser_smoke"
    - path: "tools/"
      provides: "Guard or helper checks that enforce tmp/ boundary and reproducibility workflow."
    - path: ".planning/phases/01-build-baseline-recovery/01-VALIDATION.md"
      provides: "Recorded reproducibility evidence and command trace for BUILD-03 proof."
      contains: "Per-Task Verification Map"
  key_links:
    - from: "Justfile"
      to: "//examples/bootstrap:use_parser_smoke //examples/bootstrap:select_lite_smoke //examples/bootstrap:select_from_lite_smoke //examples/bootstrap:select_rich_smoke"
      via: "repeatable command target executes full smoke suite in one deterministic invocation"
      pattern: "(use_parser_smoke|select_lite_smoke|select_from_lite_smoke|select_rich_smoke)"
    - from: "tools/"
      to: "MODULE.bazel MODULE.bazel.lock .bazelrc **/BUILD.bazel **/*.bzl"
      via: "guard scan enforces no required tmp/ path references in tracked build configuration"
      pattern: "tmp/"
    - from: ".planning/phases/01-build-baseline-recovery/01-VALIDATION.md"
      to: "BUILD-03"
      via: "validation document captures exact commands and outcomes proving tmp-independent reproducibility"
      pattern: "BUILD-03"
---

# Plan 01-02: Smoke and Reproducibility Proof

<objective>
Prove end-to-end Phase 1 baseline by running all parser smoke targets and enforcing that build/test flow does not rely on tracked source from `tmp/`.

Purpose: Turn recovered baseline into repeatable evidence-backed acceptance.
Output: Deterministic smoke execution path plus reproducibility and `tmp/` boundary proof.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add deterministic smoke-suite entrypoint for fast repeat runs</name>
  <files>Justfile, tools/</files>
  <action>Create or update a single command entrypoint that runs the required smoke labels consistently, preferring quick checks first and full run second.</action>
  <verify>
    <automated>bazel --batch --output_user_root=/tmp/bazel-root test //examples/bootstrap:use_parser_smoke //examples/bootstrap:select_lite_smoke</automated>
  </verify>
  <done>A repeatable smoke command path exists and quick subset checks return green for low-latency feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Execute full required smoke suite in one deterministic command</name>
  <files>Justfile, examples/bootstrap/BUILD.bazel</files>
  <action>Run all four required smoke targets together after baseline reset behavior is in place, then repeat from a clean-checkout worktree.</action>
  <verify>
    <automated>bazel --batch --output_user_root=/tmp/bazel-root test //examples/bootstrap:use_parser_smoke //examples/bootstrap:select_lite_smoke //examples/bootstrap:select_from_lite_smoke //examples/bootstrap:select_rich_smoke</automated>
    <automated>git worktree add /tmp/clickshack-phase01-clean-smoke HEAD && (cd /tmp/clickshack-phase01-clean-smoke && bazel --batch --output_user_root=/tmp/bazel-root-clean-smoke test //examples/bootstrap:use_parser_smoke //examples/bootstrap:select_lite_smoke //examples/bootstrap:select_from_lite_smoke //examples/bootstrap:select_rich_smoke) && git worktree remove /tmp/clickshack-phase01-clean-smoke</automated>
  </verify>
  <done>All required smoke targets pass together with exit code 0 in both active workspace and clean-checkout worktree, providing task-level proof for `BUILD-02`.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce tmp boundary guard on tracked Bazel/config sources</name>
  <files>tools/, MODULE.bazel, MODULE.bazel.lock, .bazelrc, **/BUILD.bazel, **/*.bzl</files>
  <action>Add or run guard checks that fail when tracked build/config files require `tmp/` as source-of-truth, then execute build/test commands with workspace `tmp/` absent in an isolated worktree.</action>
  <verify>
    <automated>rg -n "(^|[\"' /])tmp/" MODULE.bazel MODULE.bazel.lock .bazelrc **/BUILD.bazel **/*.bzl</automated>
    <automated>git worktree add /tmp/clickshack-phase01-no-tmp HEAD && (cd /tmp/clickshack-phase01-no-tmp && rm -rf tmp && test ! -e tmp && bazel --batch --output_user_root=/tmp/bazel-root-no-tmp build //ported_clickhouse:parser_lib && bazel --batch --output_user_root=/tmp/bazel-root-no-tmp test //examples/bootstrap:use_parser_smoke //examples/bootstrap:select_lite_smoke) && git worktree remove /tmp/clickshack-phase01-no-tmp</automated>
  </verify>
  <done>No required tracked build/config inputs depend on `tmp/`, and build/test remain green when workspace `tmp/` is absent, satisfying execution proof for `BUILD-03`.</done>
</task>

<task type="auto">
  <name>Task 4: Capture reproducibility evidence and parser-lib regression check</name>
  <files>.planning/phases/01-build-baseline-recovery/01-VALIDATION.md</files>
  <action>Record exact commands and outcomes for smoke, tmp-boundary, and parser-lib checks in validation documentation.</action>
  <verify>
    <automated>bazel --batch --output_user_root=/tmp/bazel-root build //ported_clickhouse:parser_lib && git ls-files tmp | wc -l</automated>
  </verify>
  <done>Validation doc contains reproducibility evidence; parser lib remains buildable; tracked `tmp/` file count stays zero.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] quick two-smoke test command passes
- [ ] full four-smoke test command passes
- [ ] clean-checkout full four-smoke proof passes
- [ ] tmp-boundary guard scan and tracked tmp file check pass
- [ ] tmp-absent worktree build/test proof passes
- [ ] parser-lib build remains green in same run
</verification>

<success_criteria>
- All tasks completed
- BUILD-02 smoke acceptance proven with deterministic execution
- BUILD-03 tmp-boundary and reproducibility evidence captured
- BUILD-01 remains green after smoke/repro checks
</success_criteria>
