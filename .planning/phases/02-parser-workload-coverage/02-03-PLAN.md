---
phase: 02-parser-workload-coverage
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - testdata/parser_workload/
  - tools/
  - docs/select_parser_roadmap.md
  - docs/parser_workload_scope.md
  - docs/parser_workload_reconciliation.md
  - .planning/phases/02-parser-workload-coverage/02-VALIDATION.md
requirements:
  - PARS-01
  - PARS-02
  - PARS-03
autonomous: true
must_haves:
  truths:
    - "Non-SQL inputs, MSSQL-only syntax, and deferred query-parameter forms are explicitly documented as excluded and remain rejected by the executable fixture suite."
    - "The supported/excluded scope documentation matches the checked-in fixture corpus instead of drifting into undocumented parser behavior."
    - "Phase 02 acceptance is backed by full-suite evidence: supported exact-summary checks, full-corpus deterministic repeat checks, excluded negatives, reconciliation sign-off, smoke non-regression, and parser-lib build proof."
  artifacts:
    - path: "docs/parser_workload_scope.md"
      provides: "Single source of truth for Phase 02 supported workload families, explicit exclusions, and representative examples aligned with fixture coverage."
      contains: "excluded"
    - path: "testdata/parser_workload/"
      provides: "Negative fixture matrix covering excluded non-SQL, MSSQL, and deferred query-parameter forms with stable reasons or failure-class expectations."
      contains: "excluded"
    - path: "docs/parser_workload_reconciliation.md"
      provides: "Final sign-off artifact tying the checked-in fixture corpus back to the prioritized workload source and recording approved gaps."
      contains: "sign-off"
    - path: ".planning/phases/02-parser-workload-coverage/02-VALIDATION.md"
      provides: "Recorded evidence for full Phase 02 acceptance commands and outcomes."
      contains: "Per-Task Verification Map"
  key_links:
    - from: "testdata/parser_workload/"
      to: "PARS-03"
      via: "excluded fixtures keep out-of-scope grammar rejected under executable regression coverage"
      pattern: "excluded"
    - from: "docs/parser_workload_scope.md"
      to: "PARS-02"
      via: "scope document names the supported workload subset and maps it to the checked-in fixture matrix"
      pattern: "supported"
    - from: ".planning/phases/02-parser-workload-coverage/02-VALIDATION.md"
      to: "PARS-01"
      via: "validation record captures determinism and non-regression evidence required for phase sign-off"
      pattern: "determinism|repeat|smoke"
    - from: "docs/parser_workload_reconciliation.md"
      to: "PARS-02"
      via: "reconciliation sign-off proves the final supported fixture corpus still matches the prioritized internal workload source"
      pattern: "approved|source|fixture"
---

# Plan 02-03: Exclusions, Scope Documentation, and Final Acceptance Proof

<objective>
Lock the supported/excluded scope in docs and executable negatives, then capture final acceptance evidence for the full Phase 02 contract.

Purpose: Satisfy the explicit-scope guardrail requirement so Phase 02 proves what is supported and what remains intentionally excluded.
Output: Exclusion matrix, scope documentation, reconciliation sign-off, and validation evidence covering supported, excluded, deterministic, and non-regressed behavior.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add explicit negative fixtures for out-of-scope grammar families</name>
  <files>testdata/parser_workload/, tools/</files>
  <action>Add representative excluded fixtures for non-SELECT/non-SQL inputs, MSSQL-only syntax such as `TOP`, bracketed identifiers, and table hints, plus deferred query-parameter syntax using `{...}`. Keep the set compact but explicit, and make the harness assert deterministic failure classification or reason mapping.</action>
  <verify>
    <automated>bash tools/parser_workload_suite.sh excluded --manifest testdata/parser_workload/manifest.json</automated>
  </verify>
  <done>Phase 02 has executable proof that excluded grammar families remain out of scope instead of drifting into accidental support.</done>
</task>

<task type="auto">
  <name>Task 2: Publish a scope document aligned with the fixture corpus</name>
  <files>docs/parser_workload_scope.md, docs/select_parser_roadmap.md, testdata/parser_workload/</files>
  <action>Write a focused Phase 02 scope document that names supported workload families, explicit exclusions, and any intentionally deferred forms. Update the broader SELECT parser roadmap only as needed so it does not overstate validated workload coverage beyond the fixture corpus.</action>
  <verify>
    <automated>bash tools/parser_workload_suite.sh audit-docs --manifest testdata/parser_workload/manifest.json --scope-doc docs/parser_workload_scope.md --roadmap-doc docs/select_parser_roadmap.md</automated>
  </verify>
  <done>Human-readable scope matches the executable fixture matrix, making supported and excluded parser behavior auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the full Phase 02 suite and record evidence in validation docs</name>
  <files>.planning/phases/02-parser-workload-coverage/02-VALIDATION.md, tools/, testdata/parser_workload/, docs/parser_workload_reconciliation.md</files>
  <action>Run the full supported fixture matrix, repeat-mode determinism checks across every supported fixture, excluded negatives, reconciliation/sign-off audit, `just smoke-suite`, and parser-lib build. Record the exact commands and outcomes in the phase validation document so downstream verification has a concrete acceptance trail.</action>
  <verify>
    <automated>bash tools/parser_workload_suite.sh full --manifest testdata/parser_workload/manifest.json</automated>
    <automated>bash tools/parser_workload_suite.sh audit-signoff --manifest testdata/parser_workload/manifest.json --source testdata/parser_workload/prioritized_internal_workload.json --signoff docs/parser_workload_reconciliation.md</automated>
    <automated>just smoke-suite</automated>
    <automated>bazel --batch --output_user_root=/tmp/bazel-root build //ported_clickhouse:parser_lib</automated>
  </verify>
  <done>Phase 02 validation evidence is written down and reproducible, not left as transient terminal output.</done>
</task>

<task type="auto">
  <name>Task 4: Final consistency pass across fixtures, docs, and validation criteria</name>
  <files>testdata/parser_workload/, docs/parser_workload_scope.md, docs/parser_workload_reconciliation.md, .planning/phases/02-parser-workload-coverage/02-VALIDATION.md</files>
  <action>Cross-check that every supported and excluded fixture category is represented in docs, every documented exclusion has an executable negative case, and the reconciliation sign-off agrees with the final fixture corpus and validation record. Tighten mismatches before phase execution is considered complete.</action>
  <verify>
    <automated>bash tools/parser_workload_suite.sh audit-docs --manifest testdata/parser_workload/manifest.json --scope-doc docs/parser_workload_scope.md --validation-doc .planning/phases/02-parser-workload-coverage/02-VALIDATION.md --signoff docs/parser_workload_reconciliation.md</automated>
  </verify>
  <done>Fixture corpus, scope docs, and validation evidence describe the same Phase 02 contract with no silent coverage gaps.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash tools/parser_workload_suite.sh excluded --manifest testdata/parser_workload/manifest.json` passes for the explicit negative matrix
- [ ] `docs/parser_workload_scope.md` and `docs/select_parser_roadmap.md` reflect the actual Phase 02 fixture scope
- [ ] `bash tools/parser_workload_suite.sh audit-docs --manifest testdata/parser_workload/manifest.json --scope-doc docs/parser_workload_scope.md --roadmap-doc docs/select_parser_roadmap.md` proves docs enumerate the same supported/excluded families carried by the manifest
- [ ] `bash tools/parser_workload_suite.sh full --manifest testdata/parser_workload/manifest.json` passes supported, excluded, and full-corpus determinism checks together
- [ ] `bash tools/parser_workload_suite.sh audit-signoff --manifest testdata/parser_workload/manifest.json --source testdata/parser_workload/prioritized_internal_workload.json --signoff docs/parser_workload_reconciliation.md` proves the final workload reconciliation sign-off still matches the manifest
- [ ] `just smoke-suite` remains green in the same acceptance run
- [ ] `bazel --batch --output_user_root=/tmp/bazel-root build //ported_clickhouse:parser_lib` passes in the final acceptance run
- [ ] `bash tools/parser_workload_suite.sh audit-docs --manifest testdata/parser_workload/manifest.json --scope-doc docs/parser_workload_scope.md --validation-doc .planning/phases/02-parser-workload-coverage/02-VALIDATION.md --signoff docs/parser_workload_reconciliation.md` proves validation, docs, and sign-off still match the fixture corpus at acceptance
</verification>

<success_criteria>
- All tasks completed
- PARS-03 is enforced by executable negatives and explicit documentation
- PARS-01 and PARS-02 final proof is captured in a repeatable validation record plus workload reconciliation sign-off
- Phase 02 finishes with aligned fixtures, docs, and acceptance evidence
</success_criteria>
