---
phase: 02.1-select-language-feature-completeness
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - PARS-01
  - PARS-02
  - PARS-03
files_modified:
  - ported_clickhouse/parsers/ASTSelectRichQuery.h
  - ported_clickhouse/parsers/ASTOrderByElementLite.h
  - ported_clickhouse/parsers/ASTJoinLite.h
  - ported_clickhouse/parsers/ParserSelectRichQuery.cpp
  - ported_clickhouse/parsers/SelectClausesLiteParsers.cpp
  - ported_clickhouse/parsers/TableSourcesLiteParsers.cpp
  - examples/bootstrap/parser_workload_summary.cc
  - testdata/parser_workload/
must_haves:
  truths:
    - "SELECT-head fidelity is retained in AST: WITH RECURSIVE, explicit SELECT ALL, DISTINCT ON, and TOP/WITH TIES intent are no longer parse-and-drop behavior."
    - "ORDER BY and JOIN metadata touched in this wave is persisted in AST and surfaced deterministically by parser_workload_summary."
    - "Unsupported exclusions remain explicit and deterministic: non_sql, non_select, mssql, and query_parameter stay exclusion classes unless intentionally re-scoped."
  artifacts:
    - path: "docs/select_dialect_missing_features.md"
      provides: "Authoritative gap inventory used as wave scope input."
      contains: "Coverage Conclusion"
    - path: "examples/bootstrap/parser_workload_summary.cc"
      provides: "Canonical deterministic emitter proving retained AST fields for new SELECT/JOIN/ORDER coverage."
      contains: "canonical"
    - path: "testdata/parser_workload/"
      provides: "Fixtures and expected summaries for newly supported SELECT surface plus unchanged explicit exclusions."
      contains: "supported|excluded"
  key_links:
    - from: "ported_clickhouse/parsers/ASTSelectRichQuery.h"
      to: "PARS-02"
      via: "SELECT-head fidelity fields create explicit AST destinations instead of parse-and-drop behavior"
      pattern: "with_recursive|select_all|distinct_on|top"
    - from: "ported_clickhouse/parsers/TableSourcesLiteParsers.cpp"
      to: "ported_clickhouse/parsers/ASTJoinLite.h"
      via: "JOIN locality/type parsing is persisted in AST and exposed for deterministic validation"
      pattern: "LOCAL|PASTE|join"
    - from: "testdata/parser_workload/"
      to: "PARS-03"
      via: "supported and excluded fixtures enforce both retained coverage and explicit unsupported policy boundaries"
      pattern: "supported|excluded|non_sql|non_select|mssql|query_parameter"
---

# Plan 02.1-01: SELECT Head, JOIN, and AST Contract Retention

<objective>
Define and wire AST retention targets for the first broad sweep families, then prove deterministic behavior with fixture-backed canonical summaries.

Purpose: eliminate parse-only acceptance for high-impact SELECT surface features.
Output: AST contract additions, parser hook updates, and summary/fixture evidence for wave-1 features.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement AST-backed SELECT-head and ORDER fidelity retention</name>
  <files>ported_clickhouse/parsers/ASTSelectRichQuery.h, ported_clickhouse/parsers/ASTOrderByElementLite.h, ported_clickhouse/parsers/ParserSelectRichQuery.cpp, ported_clickhouse/parsers/SelectClausesLiteParsers.cpp</files>
  <action>Add explicit retention for WITH RECURSIVE, SELECT ALL, DISTINCT ON, TOP n [WITH TIES], and in-scope ORDER BY metadata (NULLS/COLLATE/FILL/INTERPOLATE) with deterministic defaults and precedence-safe parsing.</action>
  <verify>
    <automated>bazel --batch --output_user_root=/tmp/bazel-root build //ported_clickhouse:parser_lib</automated>
    <automated>bash tools/parser_workload_suite.sh supported --manifest testdata/parser_workload/manifest.json</automated>
  </verify>
  <done>Wave-1 SELECT-head and ORDER families are retained in AST with deterministic canonical shape.</done>
</task>

<task type="auto">
  <name>Task 2: Implement JOIN locality/type completeness with canonical exposure</name>
  <files>ported_clickhouse/parsers/TableSourcesLiteParsers.cpp, ported_clickhouse/parsers/ASTJoinLite.h, examples/bootstrap/parser_workload_summary.cc</files>
  <action>Add LOCAL join modifier and PASTE JOIN mapping, persist results in ASTJoinLite, and expose join-locality/type in canonical summary output for deterministic fixture auditing.</action>
  <verify>
    <automated>bash tools/parser_workload_suite.sh supported --manifest testdata/parser_workload/manifest.json</automated>
    <automated>bash tools/parser_workload_suite.sh repeat --manifest testdata/parser_workload/manifest.json</automated>
  </verify>
  <done>JOIN completeness additions are proven by deterministic summaries across supported fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh fixtures for wave-1 support and unchanged exclusions</name>
  <files>testdata/parser_workload/, examples/bootstrap/parser_workload_summary.cc</files>
  <action>Add representative success and edge fixtures for SELECT-head and JOIN features, update expected canonical summaries, and keep exclusion fixtures for non_sql/non_select/mssql/query_parameter unchanged and explicitly classified.</action>
  <verify>
    <automated>bash tools/parser_workload_suite.sh audit --manifest testdata/parser_workload/manifest.json</automated>
    <automated>bash tools/parser_workload_suite.sh excluded --manifest testdata/parser_workload/manifest.json</automated>
  </verify>
  <done>Wave-1 fixture contract captures new support and preserves explicit unsupported policy boundaries.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `bazel --batch --output_user_root=/tmp/bazel-root build //ported_clickhouse:parser_lib //examples/bootstrap:parser_workload_summary` passes
- [ ] `bash tools/parser_workload_suite.sh audit --manifest testdata/parser_workload/manifest.json` passes
- [ ] `bash tools/parser_workload_suite.sh supported --manifest testdata/parser_workload/manifest.json` passes with updated canonical summaries
- [ ] `bash tools/parser_workload_suite.sh repeat --manifest testdata/parser_workload/manifest.json` proves determinism for touched supported fixtures
- [ ] `bash tools/parser_workload_suite.sh excluded --manifest testdata/parser_workload/manifest.json` keeps unsupported classes deterministic (`non_sql`, `non_select`, `mssql`, `query_parameter`)
- [ ] `just smoke-suite` remains green
</verification>

<success_criteria>
- All tasks completed
- Wave-1 gap families from `docs/select_dialect_missing_features.md` are retained in AST and visible in canonical summaries
- No silent fallback from supported to dropped AST payload
- Unsupported exclusions policy remains explicit, executable, and unchanged in meaning
</success_criteria>
