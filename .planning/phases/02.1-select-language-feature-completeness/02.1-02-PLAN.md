---
phase: 02.1-select-language-feature-completeness
plan: "02"
type: execute
wave: 2
depends_on:
  - "02.1-01"
autonomous: true
requirements:
  - PARS-01
  - PARS-02
  - PARS-03
files_modified:
  - ported_clickhouse/parsers/ExpressionOpsLiteParsers.cpp
  - ported_clickhouse/parsers/SelectClausesLiteParsers.cpp
  - ported_clickhouse/parsers/ASTWindowDefinitionLite.h
  - ported_clickhouse/parsers/ASTLimitLite.h
  - ported_clickhouse/parsers/ASTLimitByLite.h
  - examples/bootstrap/parser_workload_summary.cc
  - testdata/parser_workload/
  - tools/parser_workload_suite.sh
must_haves:
  truths:
    - "CASE and window-expression coverage is parser-only but AST-retentive: simple CASE and OVER(...) metadata are preserved and summarized deterministically."
    - "ORDER/GROUP/LIMIT fidelity upgrades retain NULLS/COLLATE/WITH FILL/INTERPOLATE, GROUPING SETS, and expression-capable LIMIT/OFFSET/FETCH forms where in scope."
    - "Exclusions policy remains explicit: any still-unimplemented SELECT gaps are represented as deterministic excluded fixtures, not hidden parser behavior."
  artifacts:
    - path: "ported_clickhouse/parsers/ExpressionOpsLiteParsers.cpp"
      provides: "Expression parser support for simple CASE and window application forms."
      contains: "CASE|OVER"
    - path: "ported_clickhouse/parsers/SelectClausesLiteParsers.cpp"
      provides: "Clause-level fidelity retention for ORDER/GROUP/LIMIT families."
      contains: "GROUPING SETS|ORDER BY|LIMIT"
    - path: "testdata/parser_workload/"
      provides: "Supported and excluded fixtures expressing wave-2 outcomes and remaining exclusions explicitly."
      contains: "supported|excluded"
  key_links:
    - from: "ported_clickhouse/parsers/ExpressionOpsLiteParsers.cpp"
      to: "PARS-02"
      via: "simple CASE and OVER parsing retain expression/window structure in AST-aware parser flow"
      pattern: "CASE|OVER"
    - from: "ported_clickhouse/parsers/SelectClausesLiteParsers.cpp"
      to: "ported_clickhouse/parsers/ASTLimitLite.h"
      via: "ORDER/GROUP/LIMIT clause fidelity is retained through clause parser and limit AST structures"
      pattern: "GROUPING SETS|ORDER BY|LIMIT|FETCH"
    - from: "testdata/parser_workload/"
      to: "PARS-03"
      via: "remaining unresolved items are captured as explicit excluded fixtures instead of implicit parser behavior"
      pattern: "supported|excluded|non_sql|non_select|mssql|query_parameter"
---

# Plan 02.1-02: Expressions, Windows, and Clause Fidelity Sweep

<objective>
Close the highest remaining SELECT completeness gaps by upgrading expression/window parsing and clause-fidelity retention.

Purpose: extend broad completeness from syntax acceptance into auditable AST retention and deterministic summaries.
Output: parser/AST updates for expression and clause families, plus fixture coverage and deterministic validation evidence.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement simple CASE and window application parsing</name>
  <files>ported_clickhouse/parsers/ExpressionOpsLiteParsers.cpp, ported_clickhouse/parsers/ASTWindowDefinitionLite.h, examples/bootstrap/parser_workload_summary.cc</files>
  <action>Add simple CASE (`CASE expr WHEN ...`) support, support `OVER ( ... )` and `OVER window_name` application, and keep precedence boundaries stable so clause keywords are not consumed accidentally. Expose retained structure in canonical summary output.</action>
  <verify>
    <automated>bash tools/parser_workload_suite.sh supported --manifest testdata/parser_workload/manifest.json</automated>
    <automated>bash tools/parser_workload_suite.sh repeat --manifest testdata/parser_workload/manifest.json</automated>
  </verify>
  <done>Expression/window features parse with deterministic AST/summaries and no precedence regressions on existing fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Retain ORDER/GROUP/LIMIT fidelity fields and expression-capable limit forms</name>
  <files>ported_clickhouse/parsers/SelectClausesLiteParsers.cpp, ported_clickhouse/parsers/ASTLimitLite.h, ported_clickhouse/parsers/ASTLimitByLite.h, examples/bootstrap/parser_workload_summary.cc, testdata/parser_workload/</files>
  <action>Retain NULLS FIRST/LAST, COLLATE, WITH FILL payload, INTERPOLATE values, ORDER BY ALL, GROUPING SETS, and expression-capable LIMIT/OFFSET/FETCH forms from the documented gap scope. Keep canonical summary ordering deterministic and update fixture expectations exactly.</action>
  <verify>
    <automated>bash tools/parser_workload_suite.sh supported --manifest testdata/parser_workload/manifest.json</automated>
    <automated>bash tools/parser_workload_suite.sh audit --manifest testdata/parser_workload/manifest.json</automated>
    <automated>bazel --batch --output_user_root=/tmp/bazel-root build //ported_clickhouse:parser_lib</automated>
  </verify>
  <done>ORDER/GROUP/LIMIT fidelity is no longer parse-and-drop for supported wave-2 families.</done>
</task>

<task type="auto">
  <name>Task 3: Reclassify remaining gaps explicitly as exclusions and lock deterministic behavior</name>
  <files>testdata/parser_workload/, tools/parser_workload_suite.sh</files>
  <action>For any phase-scope item still not implemented after wave-2 changes, add/update excluded fixtures with explicit exclusion class and deterministic failure expectation; do not leave silent unsupported behavior. Keep non_sql, non_select, mssql, and query_parameter policies intact unless explicitly re-scoped.</action>
  <verify>
    <automated>bash tools/parser_workload_suite.sh excluded --manifest testdata/parser_workload/manifest.json</automated>
    <automated>bash tools/parser_workload_suite.sh full --manifest testdata/parser_workload/manifest.json --source docs/select_dialect_missing_features.md --signoff docs/parser_workload_reconciliation.md</automated>
  </verify>
  <done>Every unresolved feature is intentionally and executably excluded, preserving deterministic unsupported policy handling.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash tools/parser_workload_suite.sh audit --manifest testdata/parser_workload/manifest.json` passes
- [ ] `bash tools/parser_workload_suite.sh supported --manifest testdata/parser_workload/manifest.json` passes for wave-2 families
- [ ] `bash tools/parser_workload_suite.sh repeat --manifest testdata/parser_workload/manifest.json` passes determinism checks
- [ ] `bash tools/parser_workload_suite.sh excluded --manifest testdata/parser_workload/manifest.json` passes with explicit exclusion classes
- [ ] `bash tools/parser_workload_suite.sh full --manifest testdata/parser_workload/manifest.json --source docs/select_dialect_missing_features.md --signoff docs/parser_workload_reconciliation.md` passes
- [ ] `bazel --batch --output_user_root=/tmp/bazel-root build //ported_clickhouse:parser_lib //examples/bootstrap:parser_workload_summary` passes
- [ ] `just smoke-suite` remains green
</verification>

<success_criteria>
- All tasks completed
- Wave-2 SELECT gap families are covered with parser acceptance plus AST fidelity evidence
- Remaining unsupported items are explicit and deterministic exclusions rather than undocumented behavior
- Deterministic canonical summary contract remains intact across repeated runs
</success_criteria>
